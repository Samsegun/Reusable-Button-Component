import MainButton from "./MainButton";

const ShadowButton = () => {
  return (
    <MainButton
      tagText="disableShadow"
      buttonStyles="text-white bg-lighter-blue py-2 px-4"
    />
  );
};

export default ShadowButton;
