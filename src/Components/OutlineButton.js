import MainButton from "./MainButton";

const OutlineButton = () => {
  return (
    <MainButton
      tagText='variant="outline"'
      buttonStyles="border border-lighter-blue text-lighter-blue py-2 px-5"
    />
  );
};

export default OutlineButton;
